# BeforeReturn 项目协作规范

## 1. 项目目标

在 7 天内完成一个可演示、可部署、可解释的 AI 产品 MVP：

> BeforeReturn：基于 ASOS GraphReturns 真实匿名交易数据的时尚电商退货风险预测与结账前干预平台。

产品在用户选择商品变体并准备结账时：

1. 预测当前“用户—商品变体”组合的退货风险；
2. 判断是否应该触发干预；
3. 在存在合理替代方案时，推荐预测风险更低的商品变体；
4. 向运营人员提供风险阈值、最低置信度和提示频率控制；
5. 明确区分“模型预测”与“真实因果效果”。

本项目的首要目标是按时交付完整产品闭环，而不是追求最复杂的模型或最多的功能。

---

## 2. 目标用户

### 消费者

- 希望买到更适合自己的商品；
- 不希望受到无意义或频繁的提示；
- 希望理解系统为什么推荐其他尺码或商品；
- 可以自由忽略建议并保留原选择。

### 电商商品/运营经理

- 希望提前识别可避免的退货风险；
- 希望控制干预覆盖率和用户打扰率；
- 希望理解模型触发干预的原因；
- 希望在业务规则约束下使用模型，而不是让模型自动决定一切。

---

## 3. MVP 核心体验

演示必须能够在 3 分钟内完成以下流程：

1. 打开产品首页，快速理解业务问题与模型表现；
2. 进入 Checkout Simulator；
3. 选择一个预设匿名用户、商品和商品变体；
4. 点击“评估退货风险”；
5. 查看预测概率、风险等级、模型置信度和主要风险因素；
6. 高风险且满足策略条件时，查看风险更低的替代变体；
7. 切换到 Policy Console，调整阈值并观察策略指标变化。

至少准备以下 3 个稳定预设案例：

- 高风险、高置信度、存在合理替代方案；
- 高风险、低置信度，因此系统不干预；
- 低风险，因此系统不打扰用户。

演示不得依赖现场重新训练模型、临时下载数据或调用不稳定的外部 AI API。

---

## 4. MVP 页面

### 4.1 Overview

展示：

- 一句话价值主张；
- 数据来源与规模；
- PR-AUC；
- Recall@Top 10%；
- Brier Score 或校准结果；
- “Try a checkout scenario”主按钮；
- 一段简短的数据限制说明。

首页禁止使用空泛营销文案，例如“AI赋能增长”“重新定义电商体验”。

### 4.2 Checkout Simulator

包含：

- 匿名用户选择；
- 商品与变体选择；
- 国家/市场信息；
- 预测退货概率；
- 高/中/低风险等级；
- 模型置信度；
- 是否触发干预；
- Top 3 风险因素；
- 清晰的返回与重新选择操作。

### 4.3 Safer Alternative

对比原选择与候选替代方案：

- 商品或变体；
- 尺码/颜色等差异；
- 预测退货风险；
- 相对风险变化；
- 推荐理由；
- 接受建议或保留原选择。

必须展示以下免责声明：

> 风险变化是基于历史数据的模型估计，不代表经过随机实验验证的因果效果。

如果数据无法支持“同款相邻尺码”推荐，则退化为同品牌或同商品类型下的低风险候选，不得伪造不存在的商品关系。

### 4.4 Policy Console

运营人员可调整：

- 高风险触发阈值；
- 最低模型置信度；
- 每 1,000 次结账最多提示次数；
- 是否允许推荐其他变体；
- 是否允许推荐其他商品。

展示策略变化后的离线模拟指标：

- 预计提示数量；
- 提示覆盖率；
- Recall@Policy；
- 误报数量或精度；
- 用户打扰率。

不得把离线模拟结果描述为真实减少的退货量或真实商业收益。

---

## 5. 数据规范

### 5.1 数据来源

核心数据仅使用 ASOS GraphReturns：

- 官方论文：https://arxiv.org/abs/2302.14096
- 官方数据入口：https://osf.io/c793h/

原始数据不得提交到 GitHub。仓库只保留：

- 下载说明；
- 数据字典；
- 小型、匿名、符合许可要求的演示样例；
- 数据处理代码。

### 5.2 数据真实性

不得：

- 把合成数据写成真实业务数据；
- 把低评分或负面评论直接当作退货标签；
- 强行关联不同数据集中的匿名用户或商品；
- 声称数据代表 ASOS 全站真实退货率；
- 使用或推断真实个人身份。

ASOS GraphReturns 只包含至少有过一次退货的用户。所有报告和页面必须明确说明这一 selection bias。

### 5.3 数据泄漏

这是最高优先级约束。

- 优先使用数据提供方给出的时间训练/测试划分；
- 所有用户、商品、品牌和类别聚合特征必须只使用预测时点之前的数据；
- 测试订单及其标签不得参与测试样本的历史特征计算；
- 不得使用退货发生之后才能获得的字段进行购买时预测；
- 对可能直接或间接编码标签的字段进行 leakage audit；
- 在文档中列出被删除字段及删除原因。

如果无法证明某个字段在预测时点可获得，则默认不使用。

---

## 6. 建模范围

### 6.1 必须完成

1. Logistic Regression baseline；
2. XGBoost 或 CatBoost 主模型；
3. 无泄漏关系衍生特征；
4. 概率校准；
5. SHAP 或等价的可解释性输出；
6. 稳定的离线推理产物；
7. 模型卡 Model Card。

建议关系特征：

- 用户历史购买次数；
- 用户历史退货率；
- 商品变体历史购买次数；
- 商品变体历史退货率；
- 用户×品牌历史退货率；
- 用户×商品类型历史退货率；
- 国家×商品类型历史退货率；
- 平滑后的低频类别统计；
- 仅由历史数据生成的交叉特征。

对低样本量统计使用贝叶斯平滑、最小样本阈值或回退到上一级聚合值，避免极端概率。

### 6.2 明确禁止进入第一版

- GNN；
- GraphSAGE；
- PyTorch Geometric；
- Node2Vec；
- Transformer；
- RAG；
- Agent 工作流；
- LLM 聊天框；
- 多模型路由；
- 实时在线训练；
- 自动爬虫；
- SHEIN 数据抓取；
- 因果推断结论。

以上内容只能在 MVP 完成后作为独立 v2 实验讨论，不能阻塞第一版交付。

### 6.3 评估指标

禁止只报告 Accuracy。至少报告：

- PR-AUC；
- ROC-AUC；
- F1；
- Precision；
- Recall；
- Recall@Top 10%；
- Brier Score；
- Calibration Curve；
- 混淆矩阵；
- 主要国家或商品类型切片表现。

如果不同指标之间存在权衡，优先解释产品含义，不得只选择最好看的数字。

---

## 7. 干预与推荐策略

模型概率不能直接触发推荐。干预必须同时满足：

1. 风险超过运营阈值；
2. 模型置信度达到最低要求；
3. 存在符合规则的低风险替代方案；
4. 替代方案风险下降达到最小幅度；
5. 当前提示频率没有超过策略上限。

不满足任一条件时，默认不干预。

推荐排序优先考虑：

1. 同商品的其他变体；
2. 同品牌、同商品类型；
3. 风险下降幅度；
4. 历史样本量与置信度；
5. 与原选择的最小差异。

不得为了生成推荐而伪造商品相似性、尺码关系或库存状态。

---

## 8. 技术栈

默认技术栈：

### 数据与模型

- Python 3.11 或 3.12；
- pandas；
- NumPy；
- scikit-learn；
- XGBoost 或 CatBoost，二选一；
- SHAP；
- joblib；
- Jupyter，仅用于审计和实验。

### API

- FastAPI；
- Pydantic；
- Uvicorn；
- 只提供 MVP 所需端点。

### Web

- Next.js；
- TypeScript；
- Tailwind CSS；
- shadcn/ui；
- Recharts 或轻量图表库；
- 不引入大型状态管理框架，除非已证明必要。

### 工程质量

- pytest；
- Ruff 或等价 Python lint；
- ESLint；
- 清晰的 `.env.example`；
- 所有密钥从环境变量读取；
- 不在仓库中提交密钥、原始数据和训练缓存。

未经说明不得更换主技术栈或同时引入多个功能重叠的库。

---

## 9. API 最小契约

建议端点：

- `GET /health`
- `GET /demo-scenarios`
- `GET /users/{user_id}/eligible-products`
- `POST /predict-return-risk`
- `POST /recommend-alternatives`
- `POST /simulate-policy`
- `GET /model-metrics`

每个预测响应至少包含：

- `risk_probability`
- `risk_level`
- `confidence`
- `should_intervene`
- `top_factors`
- `policy_reasons`
- `model_version`

如果模型服务不可用，前端必须显示明确错误，不得伪造随机预测结果。

---

## 10. UI 与视觉规范

整体采用“Apple 的克制留白＋抖音电商的数据反馈节奏”，但不得直接复制任何品牌页面、Logo、图标、素材或专有布局。

### 10.1 视觉关键词

- 简洁；
- 高级；
- 清晰；
- 轻量；
- 数据可信；
- 强调行动而非装饰。

### 10.2 色彩

- 主背景：白色或极浅暖灰；
- 主要文字：接近黑色；
- 次要文字：中性灰；
- 卡片边框：浅灰；
- 主强调色：克制的黑色或深色；
- 风险色：红/橙/绿，但只用于状态和关键数值；
- 可使用少量偏青绿色或粉红色作为电商反馈点缀，不得大面积高饱和渐变。

颜色不能作为唯一的信息编码方式，风险状态同时提供文字和图标。

### 10.3 排版

- 优先使用系统字体栈：`-apple-system, BlinkMacSystemFont, Inter, sans-serif`；
- 大标题简短有力；
- 正文宽度受控；
- 数据数字使用清晰的层级；
- 保持宽松留白；
- 避免密集表格和超过两层嵌套卡片。

### 10.4 组件

- 卡片圆角克制，不做过度“软萌”；
- 阴影极轻，优先使用边框和层级；
- 主按钮明确，次要操作弱化；
- 图表默认简洁，不显示无意义网格线；
- 动效时长约 150–250ms；
- 仅对状态切换、数字更新和面板展开使用动效；
- 禁止无意义视差、粒子背景、3D地球、自动轮播和大面积玻璃拟态。

### 10.5 信息设计

每个页面只突出一个主任务：

- Overview：理解产品价值；
- Checkout：完成一次风险评估；
- Alternative：做出保留或切换选择；
- Policy Console：调整干预策略。

风险信息必须同时回答：

1. 风险是多少；
2. 为什么；
3. 模型有多确定；
4. 系统准备采取什么行动；
5. 用户或运营人员可以如何覆盖该行动。

---

## 11. 可访问性与响应式

- 桌面端为主要演示环境；
- 最低支持 1280px 宽度下完整展示；
- 移动端保证基础可用，但不为移动端单独扩展功能；
- 交互元素必须支持键盘聚焦；
- 表单必须有明确标签；
- 对比度符合基本 WCAG 要求；
- loading、empty、error、low-confidence 状态均需设计；
- 禁止使用只有 hover 才能访问的重要信息。

---

## 12. 文案规范

UI 文案统一使用英文，代码注释和项目文档可使用中文或英文，但同一文件内保持一致。

文案原则：

- 不拟人化模型；
- 不说“AI知道你会退货”；
- 不使用确定性承诺；
- 不把相关性描述为因果关系；
- 不制造焦虑或强迫用户接受推荐；
- 提供清晰的“Keep original choice”选项。

推荐文案：

- `Estimated return risk`
- `Model confidence`
- `Why this was flagged`
- `A lower-risk alternative is available`
- `Keep original choice`
- `No intervention — confidence is below policy threshold`

---

## 13. 仓库结构

建议结构：

```text
before-return/
├── AGENTS.md
├── README.md
├── SPEC.md
├── STATUS.md
├── .env.example
├── data/
│   ├── README.md
│   └── samples/
├── docs/
│   ├── data-dictionary.md
│   ├── leakage-audit.md
│   ├── model-card.md
│   └── case-study.md
├── notebooks/
│   ├── 01_data_audit.ipynb
│   └── 02_model_experiments.ipynb
├── src/
│   ├── data/
│   ├── features/
│   ├── training/
│   ├── evaluation/
│   └── inference/
├── api/
├── web/
└── tests/
```

不要为了匹配该结构进行无收益的大规模重构。若仓库已有合理结构，应在现有结构上最小修改。

---

## 14. 七天执行顺序

### Day 1：数据审计

- 检查文件和字段；
- 建立数据字典；
- 确认标签和时间边界；
- 完成 leakage audit 初稿；
- 输出可复现的数据处理流程。

### Day 2：Baseline 与主模型

- Logistic Regression；
- XGBoost/CatBoost；
- 固定评估集；
- 输出完整指标。

### Day 3：关系特征与校准

- 实现无泄漏聚合特征；
- 对比模型；
- 概率校准；
- 生成 SHAP 解释；
- 固化模型产物。

### Day 4：策略与 API

- 干预规则；
- 替代方案排序；
- Policy Simulator；
- API 契约和测试。

### Day 5：核心 UI

- Overview；
- Checkout Simulator；
- Safer Alternative；
- 接通真实推理 API。

### Day 6：Policy Console 与稳定性

- 策略控制；
- 三个预设案例；
- loading/error/empty 状态；
- 修复阻塞性问题；
- 完成基础响应式。

### Day 7：验证与交付

- 全流程测试；
- README；
- Model Card；
- Case Study；
- 部署；
- 录制 3 分钟演示视频。

任何延期功能必须从范围中移除，不得挤占 Day 7 的部署和演示时间。

---

## 15. 完成标准

只有同时满足以下条件，MVP 才算完成：

1. 数据处理流程可复现；
2. 已完成并记录 leakage audit；
3. Baseline 与主模型有可比较指标；
4. 概率经过校准或明确记录未校准原因；
5. 三个预设案例均可稳定运行；
6. 前端调用真实模型 API，不使用随机结果；
7. 高风险案例可展示合理替代方案；
8. 低置信度案例不会触发干预；
9. Policy Console 可以改变策略输出；
10. 页面包含数据限制与非因果免责声明；
11. 核心测试通过；
12. README、Model Card 和 Case Study 已完成；
13. Demo 可通过公开链接或一条本地命令启动；
14. 3 分钟演示流程无需临场修复。

---

## 16. Codex 工作方式

每次开始工作前必须：

1. 阅读本文件、`SPEC.md` 和 `STATUS.md`；
2. 检查当前仓库状态和未提交变更；
3. 明确本次只完成哪一个里程碑；
4. 优先复用已有代码，避免无收益重写。

每次修改时必须：

- 保持改动小而可验证；
- 不修改无关文件；
- 不覆盖用户已有修改；
- 不添加范围外功能；
- 不因“最佳实践”进行大规模重构；
- 不在一个步骤中同时替换前端框架、API框架和模型；
- 遇到阻塞时先说明根因和最小修复方案；
- 对不可逆操作先停止并请求确认。

每个里程碑结束后必须：

1. 运行相关测试或验证命令；
2. 更新 `STATUS.md`；
3. 记录已完成、验证结果、已知问题和下一步；
4. 不把未验证功能标记为完成。

如果用户提出的新需求可能影响一周交付，先说明对时间和范围的影响，再等待用户决定替换哪个现有功能。不得默认扩大范围。

---

## 17. 决策优先级

发生冲突时，按以下顺序决策：

1. 数据真实性和隐私；
2. 防止数据泄漏；
3. Demo 稳定可运行；
4. 产品逻辑可信；
5. 一周内可交付；
6. 模型性能；
7. UI 细节；
8. 技术新颖性。

如果复杂方案与按时交付冲突，选择更简单、可解释、可验证的方案。
